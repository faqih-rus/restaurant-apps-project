import CONFIG from '../globals/config.js';

const API_ENDPOINT = {
  LIST_RESTAURANTS: `${CONFIG.BASE_URL}${CONFIG.API_ENDPOINTS.LIST}`,
  DETAIL_RESTAURANT: (id) => `${CONFIG.BASE_URL}${CONFIG.API_ENDPOINTS.DETAIL(id)}`,
  SEARCH_RESTAURANT: (query) => `${CONFIG.BASE_URL}${CONFIG.API_ENDPOINTS.SEARCH(query)}`,
  POST_REVIEW: `${CONFIG.BASE_URL}${CONFIG.API_ENDPOINTS.REVIEW}`,
};

export default API_ENDPOINT;
